/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the deprecated appDir experimental option
  // It's now stable and doesn't need to be specified
}

module.exports = nextConfig